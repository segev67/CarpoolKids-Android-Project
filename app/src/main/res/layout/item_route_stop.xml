<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingVertical="@dimen/spacing_sm">

    <!-- Numbered chip: same blue as the polyline / marker so the list and the map line up visually. -->
    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/item_route_stop_number"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:background="@drawable/bg_route_stop_chip"
        android:gravity="center"
        android:textColor="@color/white"
        android:textSize="@dimen/text_section_label_size"
        android:textStyle="bold"
        tools:text="1" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/spacing_md"
        android:layout_weight="1"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <com.google.android.material.textview.MaterialTextView
                android:id="@+id/item_route_stop_name"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:ellipsize="end"
                android:maxLines="1"
                android:textColor="@color/dashboard_text_primary"
                android:textDirection="ltr"
                android:textSize="@dimen/text_title_size"
                android:textStyle="bold"
                tools:text="Daniel" />

            <com.google.android.material.textview.MaterialTextView
                android:id="@+id/item_route_stop_you_badge"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="@dimen/spacing_sm"
                android:background="@color/button_primary_blue"
                android:paddingHorizontal="@dimen/spacing_sm"
                android:paddingVertical="2dp"
                android:text="@string/route_you_badge"
                android:textColor="@color/white"
                android:textSize="@dimen/text_micro_size"
                android:textStyle="bold"
                android:visibility="gone"
                tools:visibility="visible" />
        </LinearLayout>

        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/item_route_stop_action"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/dashboard_text_secondary"
            android:textSize="@dimen/text_section_label_size"
            tools:text="Pick up · 16:42" />

        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/item_route_stop_distance"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/dashboard_text_secondary"
            android:textSize="@dimen/text_caption_size"
            tools:text="1.2 km from previous" />
    </LinearLayout>

</LinearLayout>
